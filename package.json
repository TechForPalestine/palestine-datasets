{
  "name": "palestine-numbers",
  "private": true,
  "repository": "https://github.com/TechForPalestine/palestine-numbers",
  "license": "MIT",
  "workspaces": [
    "site"
  ],
  "scripts": {
    "casualties-daily": "bun run scripts/casualties-daily.ts",
    "docs-start": "cd site && bun run start",
    "docs-build": "./scripts/pre-build.sh && cd site && bun run build && cd .. && ./scripts/pre-deploy.sh",
    "docs-serve": "cd site && bun run serve"
  },
  "devDependencies": {
    "@types/node": "^20.11.0"
  },
  "dependencies": {
    "react-loadable": "npm:@docusaurus/react-loadable@^5.5.2"
  }
}
