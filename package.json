{
  "name": "palestine-numbers",
  "private": true,
  "repository": "https://github.com/TechForPalestine/palestine-numbers",
  "license": "MIT",
  "workspaces": [
    "site"
  ],
  "scripts": {
    "casualties-daily": "yarn scripts/casualties-daily.ts",
    "docs-start": "pushd site && yarn start",
    "docs-build": "pushd site && yarn build && popd && ./scripts/pre-deploy.sh",
    "docs-serve": "pushd site && yarn serve"
  },
  "devDependencies": {
    "@types/node": "^20.11.0"
  }
}
