{
  "name": "palestine-numbers",
  "private": true,
  "repository": "https://github.com/TechForPalestine/palestine-numbers",
  "license": "MIT",
  "workspaces": [
    "site"
  ],
  "scripts": {
    "casualties-daily": "npm scripts/casualties-daily.ts",
    "docs-start": "cd site && npm start",
    "docs-build": "./scripts/pre-build.sh && cd site && npm run build && cd .. && ./scripts/pre-deploy.sh",
    "docs-serve": "cd site && npm serve"
  },
  "devDependencies": {
    "@types/node": "^20.11.0"
  }
}
